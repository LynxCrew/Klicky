[gcode_macro _HOMING_VARIABLES]
variable_pos_z_home: 0.00, 0.00

variable_zhop: 20

# Defines the run current that should be used when moving the nozzle away from the bed whil zhopping
# (To prevent the z-steppers from locking up if they reach the physical limit)
# variable_zhop_run_current: 0.2

# Defines the type of tmc driver you are using for z
# Used by the macro to find all z steppers
# variable_driver_type: 'tmc2209'

# Verbosity
# variable_zhop_verbose: 'low'

# DO NOT EDIT
# Defines the Version of the variables to prevent compatibility problems
variable_version: 1

gcode:
